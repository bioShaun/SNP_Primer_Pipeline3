# SNP_Primer_Pipeline3 测试分层策略建议

## 文档目的

基于当前测试目录结构和代码审阅结果，给出一套测试分层方案。

核心目标：在重构之前把测试补齐，为后续重构提供安全网。

本文档描述**策略与落地进度**；具体实现以仓库内测试与 `pyproject.toml` 为准。

## 落地进度（2026-04）

以下对照上文「建议的落地顺序」与 P0/P1 清单，标注当前完成度。

### 已完成的项

| 类别 | 内容 |
|------|------|
| 立即可做 | `tests/consistency/conftest.py`：`v2_examples` 缺失时改为 **`pytest.fail`**（不再静默 skip）。 |
| 立即可做 | **`pyproject.toml`**：启用 **`--strict-markers`**；声明 `unit` / `integration` / `e2e` / `smoke` / `manual` / `slow` 等 marker。 |
| 重命名 | `tests/test_integration.py` → **`tests/test_module_basics.py`**；`tests/consistency/test_e2e_consistency.py` → **`tests/consistency/test_pipeline_smoke.py`**。 |
| 合成夹具 | 新增 **`tests/fixtures/`** 包（占位）；P0/P1 单元测试主要在测试文件内联短序列，**不依赖** `v2_examples` 与外部工具链即可运行。 |
| P0 单元测试 | 新增 **`tests/test_unit_blast_flanking.py`**（`_calculate_subject_snp_position`、`_create_flanking_region`、`_parse_query_id` 等）。 |
| P0 单元测试 | 新增 **`tests/test_unit_alignment_variant.py`**（`find_variant_sites_v2`：无 homeolog、缺 target、双序列差异等）。 |
| P0 单元测试 | 新增 **`tests/test_unit_kasp_filter.py`**（`_filter_primers_v2`）。 |
| P0 单元测试 | 新增 **`tests/test_unit_reverse_complement.py`** 与 **`tests/test_unit_caps_enzyme.py`**（三份 RC 行为差异、`_test_enzyme` 等）。 |
| P1 单元测试 | 新增 **`tests/test_unit_parser_p1.py`**（`detect_format`、`parse_coordinates` + mock `_fetch_sequences`）。 |
| P1 单元测试 | 新增 **`tests/test_unit_primer3_parser_p1.py`**（`Primer3OutputParser.parse_string`）。 |
| 已知问题 xfail | 新增 **`tests/test_regression_process_snp_target.py`**：`process_snp` 应按 SNP 染色体选择模板序列，**`@pytest.mark.xfail`**（当前实现仍取第一条）。 |
| 重构即修 | **`core/alignment.py`**：`MultipleSequenceAligner.align()` 清理临时文件由裸 `except:` 改为 **`except OSError:`**。 |
| Marker 落地 | 根目录与 **`tests/consistency/*`** 已按层级打上 **`unit` / `integration` / `e2e` / `slow` / `smoke` / `consistency`** 等；`test_end_to_end.py` 使用 **`e2e` + `slow`**。 |
| 工程脚本 | **`scripts/generate_consistency_report.py`** 中一致性用例名已改为 **`test_pipeline_smoke`**。 |
| 代码风格 | **`[tool.ruff.lint.per-file-ignores]`**：对 `tests/**/test_*.py` 忽略 `PLR2004`，避免测试断言中的「魔法数字」告警。 |

### 部分完成或未完成的项

| 类别 | 状态 | 说明 |
|------|------|------|
| P0 `_check_dcaps` | 部分 | 仅覆盖 `_test_enzyme` 等路径；**未**单独为 `_check_dcaps` 全分支补测（`caps.py` 中 `_check_dcaps` 与递归相关逻辑仍需谨慎）。 |
| P1 KASP 输出 / Primer3 输入 | 部分 | 已新增 **`tests/test_unit_kasp_format_output.py`**（`format_output` / `format_simple_output` 的表头、字段格式、specificity/best-primer 行为、variant 附加块）与 **`tests/test_unit_kasp_primer3_inputs.py`**（`_generate_primer3_inputs_v2` 的 SNP 跳过、产物长度上限、force left/right end、V2 默认参数、`pick_anyway`）。`_convert_to_v2_format` / `_format_primer_pair_v2` 仍通过 E2E 间接覆盖，未单独补测。 |
| P1 `find_variant_sites`（V1） | 未做 | 仍以 E2E/consistency 间接覆盖为主。 |
| P2 | 未做 | `config`、`detect_format` 全覆盖、`_get_homeo_seq` 等仍按原文档列为可选。 |
| 删除空文件 | 不适用 / 已无需 | **`test_kasp_consistency_fixed.py`** 在仓库中已不存在，未再单独提交删除。 |
| CI 二道防线 | 未做 | **skipped 计数阈值**、专用 workflow：仓库内**尚无** `.github/workflows`，未接入自动化门禁。 |
| `tests/manual/*` | 未打 marker | 当前为**脚本式**入口（`main()`），**不由 pytest 收集**，故未加 `@pytest.mark.manual`（若未来改为 pytest 用例再补）。 |

### 测试规模（验收时参考）

全量 `pytest`（含 E2E、consistency）：约 **133** 条用例，默认子集（`-m "not e2e and not slow"`）下 **100 passed + 1 xfailed**（xfail 对应上文 `process_snp` 目标序列选择，其余为 E2E / slow 用例）。快速子集示例：`pytest -m "not e2e and not slow"` 可跳过 E2E 与标记为 `slow` 的 integration 类用例。

> **说明**：下文「当前现状」中的覆盖率数字与「90 passed」来自文档撰写时的基线；合并 P0/P1 单测与 marker 后，**以本地 `pytest` 与覆盖率报告为准**。

## 当前现状

### 覆盖率概览

以下数据来自 `python -m pytest -q` 全量运行（90 passed, 105s），包含 Unit、Integration、Consistency、E2E 全部测试。

总覆盖率 **70%**（2246 行，665 行未覆盖）。

| 模块 | 覆盖率 | 说明 |
|------|--------|------|
| `core/specificity.py` | 89% | 已有较完整的显式 unit 测试 |
| `models.py` | 85% | 数据模型，覆盖良好 |
| `core/alignment.py` | 85% | 主要由 E2E 和 consistency 测试间接覆盖 |
| `primers/kasp.py` | 79% | 主要由 E2E 测试间接覆盖，缺少显式 unit 测试 |
| `core/blast.py` | 78% | 主要由 E2E 测试间接覆盖，缺少显式 unit 测试 |
| `main.py` | 71% | 主流程编排，由 E2E 测试覆盖 |
| `core/primer3_parser.py` | 69% | Primer3 接口层 |
| `config.py` | 63% | 配置管理 |
| `exceptions.py` | 62% | 异常类 |
| `core/parser.py` | 42% | coordinate 模式未覆盖 |
| `primers/caps.py` | 24% | 覆盖最低的核心模块 |

关键观察：

- 多数模块的覆盖率来自 E2E 和 consistency 测试的**间接执行**，而非**显式 unit 测试**。
- 间接覆盖意味着：这些行被执行过，但没有针对性的断言来锁定其行为。重构时如果改变了中间逻辑但最终输出不变，间接覆盖不会捕获回归。
- `caps.py`（24%）和 `parser.py`（42%）是覆盖率最低的两个核心模块。

### 现有测试文件审查

#### 1. `tests/test_basic_structure.py` — 实际层级：Unit

内容：验证 import、config 创建、异常类、model 序列化。

评估：名副其实的 Unit 测试，无问题。

#### 2. `tests/test_blast_query_id_parsing.py` — 实际层级：Unit

内容：测试 `FlankingExtractor.extract_flanking_regions` 对含下划线 query ID 的解析。

评估：针对单一纯逻辑的 Unit 测试，无问题。

#### 3. `tests/test_specificity.py` — 实际层级：Unit

内容：测试 BTOP 解析、3' 端匹配检测、Fail/Warning/PASS 逻辑、best primer 选择、FASTA 准备。

评估：质量较高的 Unit 测试，覆盖了 specificity 模块的核心逻辑。

#### 4. `tests/test_module_basics.py`（原 `test_integration.py`）— 实际层级：Unit

内容：

- `test_polymarker_parser_integration` — 单模块解析，无跨模块协作
- `test_kasp_designer_initialization` — 仅验证构造函数
- `test_caps_designer_with_enzyme_file` — 单模块酶库加载
- `test_snp_model_serialization` — dataclass 序列化
- `test_package_imports` — import 验证

评估：所有测试函数都只涉及单模块、无跨模块协作，实际是 Unit 级别。原文件名 `test_integration` 曾造成语义误导。

**进度**：已重命名为 `test_module_basics.py`。

#### 5. `tests/test_end_to_end.py` — 实际层级：E2E

内容：使用 `run_pipeline()` 跑主流程，检查输出文件、BLAST 结果、flanking sequences、SNP 目录。

评估：当前最接近正式 E2E 测试的文件。但每个测试函数都独立调用 `run_pipeline()`，存在重复执行开销。

#### 6. `tests/consistency/` — 实际层级：混杂

各文件实际层级不同，需要区分对待：

| 文件 | 实际层级 | 说明 |
|------|---------|------|
| `test_pipeline_smoke.py`（原 `test_e2e_consistency.py`） | Smoke | 只做 import、config 创建、文件结构验证，多处写了 "Skip actual pipeline execution" |
| `test_parser_consistency.py` | Integration | 构造输入 → 跑 parser → 验证输出，真正的跨步骤验证 |
| `test_kasp_consistency.py` | Integration | 依赖 Primer3 和 V2 参考数据，实际运行 KASP 设计并验证结果 |
| `test_kasp_consistency_fixed.py` | 死文件 | 空文件，应删除 |
| `test_blast_consistency.py` | Smoke / Reference-validation | 明确写了 "Skip actual BLAST execution for now"，只做类实例化和 V2 参考数据属性校验 |
| `test_alignment_consistency.py` | 待确认 | 依赖 V2 参考数据 |
| `test_flanking_consistency.py` | 待确认 | 依赖 V2 参考数据 |
| `test_caps_consistency.py` | Smoke / Reference-validation | 不执行 V3 CAPS 设计，只校验 V2 参考数据属性（DNA 合法性、位置、Tm 范围等） |

共性问题：所有 consistency 测试依赖 `test_data/v2_examples` 目录。**进度**：`examples_dir` fixture 已在 `conftest.py` 中改为 **`pytest.fail`**，缺失时不再静默通过（仍可能有单测内因工具不可用而 `pytest.skip` 的路径，见「落地进度」）。

#### 7. `tests/manual/` — 实际层级：Manual

- `test_full_pipeline.py` — 从头跑完整 V3 pipeline
- `test_e2e_real.py` — 借助 V2 中间产物比较

评估：高成本、强环境依赖，不适合默认自动执行。

## 核心问题

当前测试体系有两层问题。

### 第一层：语义混乱（原始报告已覆盖）

1. `e2e` 这个词被不同类型测试复用。
2. 原 `test_integration.py`（现为 `test_module_basics.py`）实际是 Unit。
3. `consistency` 目录内部各文件层级不同但被统一对待。
4. pytest marker 已声明但没有成为稳定约定。

### 第二层：覆盖缺位（原始报告未覆盖）

1. 核心业务模块（caps 24%、parser 42%）覆盖率偏低；其余模块（kasp 79%、blast 78%、alignment 85%）虽然覆盖率较高，但主要来自 E2E 间接执行，缺少显式 unit 测试来精确锁定行为。
2. 关键纯函数（`_calculate_subject_snp_position`、`find_variant_sites_v2`、`_reverse_complement` 三份实现）已被 E2E 间接执行，但没有针对性的显式 unit 测试。
3. 已知的正确性问题（target 序列选择脆弱）**进度**：已有 **`@pytest.mark.xfail`** 用例描述期望行为（见 `tests/test_regression_process_snp_target.py`）。
4. 空文件 `test_kasp_consistency_fixed.py`：**进度**：仓库中已不存在，无需删除。

## 重构前补测试的特殊约束

在"先补测试、再重构"的场景下，需要特别注意一个问题：

**不要为已知有 bug 的行为编写"保护性测试"。**

评估报告 #1 已经确认：`process_snp()` 中 target 序列的选择依赖"第一条命中"，这是一个正确性风险。如果先写测试锁定当前行为，重构修正 bug 时这些测试会失败，产生"绿变红"的误导。

正确做法：

- 对已知有 bug 的行为：写测试描述**期望的正确行为**（TDD 风格，先红后绿），用 `@pytest.mark.xfail(reason="已知问题 #1：target 序列选择脆弱")` 标记。
- 对行为正确但缺少测试的纯函数：写测试锁定当前正确行为。
- 对行为不确定的逻辑：先审查确认行为是否正确，再决定测试策略。

## 推荐的测试分层

### 1. Unit

定义：

- 针对单个函数、类或局部逻辑。
- 不依赖完整流程，不依赖真实外部工具链（BLAST、Primer3、Muscle）。
- 纯函数测试应 mock 所有外部依赖。

适用内容：

- query id 解析、IUPAC 转换、reverse complement
- `_calculate_subject_snp_position`、`_create_flanking_region` 等纯函数
- BTOP 解析、3' 端匹配检测
- Primer3 输出解析
- 数据模型序列化

### 2. Integration

定义：

- 覆盖多个模块协作，**实际执行了 V3 代码的跨模块逻辑**。
- 可以使用测试数据、受控输入、局部真实依赖。
- 不要求完整从用户输入跑到最终产物。

不属于 Integration 的情况：

- 仅做类实例化或 import 验证 → Smoke。
- 仅校验 V2 参考数据的属性（如 DNA 合法性、Tm 范围）但不执行 V3 逻辑 → Reference-validation。

适用内容：

- parser → FASTA 生成链路
- BLAST 结果解析 → flanking region 提取链路
- alignment → variant site 检测链路
- KASP/CAPS 设计的子链路验证
- consistency 目录中实际执行了 V3 跨模块逻辑的测试（如 `test_parser_consistency.py`、`test_kasp_consistency.py`）

### 3. E2E

定义：

- 从用户可见入口开始。
- 调用真实主流程 `run_pipeline()`。
- 验证关键输出是否生成且满足预期。

适用内容：

- 使用示例输入文件和测试数据库，完整跑一遍流程
- 验证中间文件和最终文件存在、结构正确、关键结果可用

建议归类：

- `tests/test_end_to_end.py`

### 4. Manual / Real-world

定义：

- 依赖真实外部环境、真实数据库、真实工具链或历史参考输出。
- 运行成本高，不适合作为默认 PR 回归集。

建议归类：

- `tests/manual/test_full_pipeline.py`
- `tests/manual/test_e2e_real.py`

## Unit 测试补齐优先级

这是本方案的核心增量内容。按覆盖率和业务关键性排序。

### P0：必须在重构前补齐

这些函数可能已被 E2E 测试间接执行，但缺少针对性的显式 unit 测试来精确锁定其行为。间接覆盖不能可靠捕获重构引入的中间逻辑变化。

| 目标函数/模块 | 所在文件 | 当前状态 | 补测要点 |
|--------------|---------|---------|---------|
| `_calculate_subject_snp_position` | `core/blast.py` | 被 E2E 间接执行，无显式 unit 测试 | 正链/负链、有 gap/无 gap、边界位置 |
| `_create_flanking_region` | `core/blast.py` | 被 E2E 间接执行，无显式 unit 测试 | 正链/负链、边界裁剪、`snp_position_in_region` 计算 |
| `_parse_query_id` / `_parse_query_id_with_known_snps` | `core/blast.py` | 有 1 个显式 unit 测试 | 补充：含下划线染色体名、畸形 ID、多种分隔符 |
| `_reverse_complement`（3 份实现） | `models.py` / `kasp.py` / `caps.py` | 无显式 unit 测试 | 纯 ATGC、含 IUPAC、空串；**必须验证三份实现的行为差异**。这些测试的目的是锁定差异、为后续合并三份实现提供依据，合并完成后应整理为单一实现的测试集，避免三份保护伞阻碍重构 |
| `find_variant_sites_v2` | `core/alignment.py` | 被 E2E 间接执行，无显式 unit 测试 | 双序列/多序列、SNP 左侧/右侧变异、无变异情况 |
| `_filter_primers_v2` | `primers/kasp.py` | 被 E2E 间接执行，无显式 unit 测试 | 过滤逻辑、diffarray 交互、边界条件 |
| `_test_enzyme` / `_check_dcaps` | `primers/caps.py` | **连间接覆盖都没有**（caps.py 整体仅 24%） | CAPS/dCAPS 酶筛选逻辑；从 P1 升级至 P0，理由见下方 |

> **关于 `caps.py` 升 P0 的说明**：P0 的定义是"重构前必须补齐的安全网"。`caps.py` 是全仓覆盖率最低的核心模块（24%），`_test_enzyme` / `_check_dcaps` 是 CAPS 设计正确性的核心判据。其他 P0 项目至少被 E2E happy path 间接执行过，而 CAPS 连间接覆盖都没有。如果继续以 24% 的覆盖率进入重构，没有任何安全网可言。

### P1：重构前应补

| 目标函数/模块 | 所在文件 | 当前状态 | 补测要点 |
|--------------|---------|---------|---------|
| `_generate_primer3_inputs_v2` | `primers/kasp.py` | 被 E2E 间接执行，无显式 unit 测试 | 变异位点→Primer3 输入生成 |
| `_convert_to_v2_format` | `primers/kasp.py` | 被 E2E 间接执行，无显式 unit 测试 | 格式转换、genomic 坐标计算、正链/负链 |
| `find_variant_sites`（V1 版） | `core/alignment.py` | 被间接执行，无显式 unit 测试 | gap 边界、单序列退化 |
| `parse_coordinates` | `core/parser.py` | 无覆盖（parser.py 对应行未执行） | coordinate 格式解析、无效输入处理 |
| `Primer3OutputParser.parse_string` | `core/primer3_parser.py` | 被间接执行，无显式 unit 测试 | Primer3 输出解析、多对/零对结果 |
| `format_output` / `format_simple_output` | `primers/kasp.py` | 被 E2E 间接执行，无显式 unit 测试 | 输出格式正确性（可用 snapshot 测试） |

### P2：重构前可选

| 目标函数/模块 | 所在文件 | 当前覆盖 | 补测要点 |
|--------------|---------|---------|---------|
| `PipelineConfig.__post_init__` | `config.py` | 部分覆盖 | 边界值验证 |
| `PipelineConfig.from_yaml` | `config.py` | 未覆盖 | YAML 加载、异常路径 |
| `SoftwarePaths.auto_detect` | `config.py` | 未覆盖 | 需要 mock 文件系统 |
| `detect_format` | `core/parser.py` | 未覆盖 | polymarker / coordinates / 未知格式 |
| `_get_homeo_seq` | `core/alignment.py` | 未覆盖 | V2 兼容的 homeolog 序列提取 |

### 已知 bug 对应的 xfail 测试

以下测试应用 `@pytest.mark.xfail` 标记，描述期望行为而非当前行为：

| 测试目标 | 对应评估报告问题 | 期望行为 |
|---------|----------------|---------|
| target 序列应为目标染色体上的命中 | #1 target 序列选择脆弱 | 当 flanking 列表顺序不以目标染色体在先时，仍应正确选择用于设计的模板序列 |

**进度**：已在 `tests/test_regression_process_snp_target.py` 中落地（mock `KASPDesigner.design_primers` 捕获传入的 `target_sequence`）。

> **关于 `bare except` 的处置调整**：评估报告 #10（`alignment.py` 的 bare except 会吞掉 `KeyboardInterrupt`）原本列入 xfail 候选，但这不是正常意义的行为测试——需要在 teardown 路径 mock 出 `side_effect=KeyboardInterrupt` 才能验证，写起来成本远高于修复本身。**进度**：`MultipleSequenceAligner.align()` 中临时文件清理已改为 **`except OSError:`**（不另立 xfail）。

## 对当前文件的归类和处置建议

### 重命名

| 原文件 | 现文件 | 原因 |
|---------|--------|------|
| `tests/test_integration.py` | `tests/test_module_basics.py` | 实际是 Unit 级别，命名误导（**已完成**） |
| `tests/consistency/test_e2e_consistency.py` | `tests/consistency/test_pipeline_smoke.py` | 不是 E2E，只做 import 和 config 创建（**已完成**） |

### 删除

| 文件 | 原因 | 进度 |
|------|------|------|
| `tests/consistency/test_kasp_consistency_fixed.py` | 空文件，无内容 | 仓库中已不存在，未再单独处理 |

### 保留不变

| 文件 | 层级 |
|------|------|
| `tests/test_basic_structure.py` | Unit |
| `tests/test_blast_query_id_parsing.py` | Unit |
| `tests/test_specificity.py` | Unit |
| `tests/test_end_to_end.py` | E2E |
| `tests/consistency/test_parser_consistency.py` | Integration |
| `tests/consistency/test_kasp_consistency.py` | Integration |
| `tests/consistency/test_blast_consistency.py` | Smoke / Reference-validation |
| `tests/consistency/test_alignment_consistency.py` | 待确认 |
| `tests/consistency/test_flanking_consistency.py` | 待确认 |
| `tests/consistency/test_caps_consistency.py` | Smoke / Reference-validation |
| `tests/manual/test_full_pipeline.py` | Manual |
| `tests/manual/test_e2e_real.py` | Manual |

### consistency 目录静默跳过风险处置

所有 consistency 测试依赖 `test_data/v2_examples`，数据不存在时 `pytest.skip` 会导致 CI 中 0 个测试实际执行却显示通过。

注意：`--strict-markers` 只能防止未声明的 marker，不能检测 session fixture 中 `pytest.skip()` 导致的整批跳过。

处置要求（**两道防线都要做，不是二选一**）：

1. **硬约束**：修改 `tests/consistency/conftest.py`，把 "v2_examples 缺失" 由 `pytest.skip` 改为 `pytest.fail`。这是唯一能真正防止静默通过的写法。`test_data/v2_examples` 是已知依赖，不应作为可选项。
2. **二道防线**：CI 中启用 `--strict-markers`，并对 collected / skipped 计数设阈值，当 consistency 测试整体被跳过时告警或失败。

两道防线的分工：防线 1 堵住本地和 CI 的路径缺失，防线 2 堵住未来新增的其他条件化 skip（例如 `primer3_core not available`）被忽视的情况。

## Marker 策略建议

### 推荐 marker

- `@pytest.mark.unit`
- `@pytest.mark.integration`
- `@pytest.mark.e2e`
- `@pytest.mark.slow`
- `@pytest.mark.manual`

### 推荐约定

- `unit`：默认开发时高频运行
- `integration`：PR 默认运行
- `e2e`：在具备外部工具环境时运行
- `manual`：不进入默认 CI，仅手工触发

### 最小落地建议

1. 给 `tests/test_end_to_end.py` 增加 `@pytest.mark.e2e`。（**已完成**，并加 `slow`）
2. 给 `tests/manual/*` 增加 `@pytest.mark.manual`。（**未做**：当前为脚本入口，非 pytest 收集；见「落地进度」）
3. 给 `tests/consistency/*` 按内容分别标记为 `integration` 或 `smoke`。（**已完成**；`test_pipeline_smoke` 已替代原 `test_e2e_consistency` 命名）
4. 给新增的 Unit 测试统一标记 `@pytest.mark.unit`。（**已完成**）

## CI / 执行策略建议

### 选用黑名单 marker 过滤，不用白名单

历史草案曾建议 `pytest -m "unit or integration or e2e"`。这属于**白名单**策略，存在一个关键问题：任何未打 marker 的新测试会被**静默排除**，本质上和当前 consistency 目录的静默跳过是同一类陷阱。忘打 marker 不会在 CI 里表现为失败，只会表现为"覆盖少了但没人发现"。

因此统一采用**黑名单 marker**策略：新测试默认会被跑到，只有显式标记为 `manual` / `slow` 的才会被排除。配合 `--strict-markers` 可以同时约束 marker 拼写。

### 本地开发默认

```bash
pytest -m "not manual and not slow" --strict-markers
```

目的：快速反馈，支持 TDD；未标记的测试默认会跑。

### PR / 常规 CI

```bash
pytest -m "not manual" --strict-markers
```

目的：保持主干稳定；包含 e2e 和 slow 但排除高成本 manual。

### 发布前 / 定时任务

```bash
pytest --strict-markers  # 全量运行，包括 manual
```

目的：在更接近真实环境的条件下做高成本验证。

## 建议的落地顺序

### 立即可做（零风险、零争议）

1. **删除空文件** `tests/consistency/test_kasp_consistency_fixed.py`。（**跳过**：文件已不存在）
2. **conftest 硬化**：`tests/consistency/conftest.py` 中把 `v2_examples` 缺失由 `pytest.skip` 改为 `pytest.fail`。（**已完成**）
3. **`pyproject.toml` 启用 `--strict-markers`**，并在 `[tool.pytest.ini_options].markers` 下声明 `unit` / `integration` / `e2e` / `smoke` / `manual` / `slow`。（**已完成**）

这三步互不冲突，与后续任何决定都兼容，且能立刻把 CI 的静默风险降到最低。

### 重构前的最小安全网

4. **构造合成 fixture**：在 `tests/fixtures/` 下准备一组最小合成输入（几条短序列 + 人工 BLAST 输出样本），让 P0 / P1 的 unit 测试完全不依赖 `v2_examples` 和外部工具链。（**部分完成**：已建 `tests/fixtures/` 包；P0/P1 单测以内联序列为主，**未**单独提交「人工 BLAST 输出样本」文件）
5. **重命名** `test_integration.py` → `test_module_basics.py`，`test_e2e_consistency.py` → `test_pipeline_smoke.py`。（**已完成**）
6. **补 P0 Unit 测试**（`_calculate_subject_snp_position`、`_reverse_complement` 三份实现、`_test_enzyme` / `_check_dcaps` 等）。（**大部分完成**；`_check_dcaps` 全分支见「落地进度」）
7. **写 xfail 测试**（target 序列选择等已知问题）；同时把 `alignment.py` 的 bare except 作为"重构即修"项一并处理，不另立 xfail。（**已完成**）

步骤 1-7 完成后构成重构的最小安全网。

### 重构过程中逐步完成

8. **补 P1 Unit 测试**（`_generate_primer3_inputs_v2`、`_convert_to_v2_format` 等）。（**部分完成**：已补 `parse_coordinates` / `detect_format` / `Primer3OutputParser.parse_string`、`format_output` / `format_simple_output`、`_generate_primer3_inputs_v2`；`_convert_to_v2_format` / `_format_primer_pair_v2` 仍依赖 E2E 间接覆盖）
9. **给所有测试文件补 marker**。（**已完成**；`tests/manual/*` 除外，见上）
10. **接入 CI 分层执行策略**（黑名单 marker + skipped 计数阈值）。（**未做**：本地/文档可照 `pytest -m "not manual"` 等执行；**skipped 计数门禁**与 **workflow 未接入**）

## 最终建议

当前项目的测试问题不只是"语义混乱"，更关键的是"核心业务逻辑的 Unit 覆盖严重不足"，以及多处**静默通过**风险（consistency 的条件 skip、未标记测试被白名单过滤）。

**进度摘要**：上述第 1、3、4 项及 P0 主体、marker 与重命名已**落地**（见「落地进度」）；**仍建议优先**补齐 P1 中 KASP 输出与 Primer3 输入链路的单测、CI 的 skipped 阈值，以及（可选）P2 与 `_check_dcaps` 深度分支。

重构前最值得做的事情，按优先级排序：

1. 先做"立即可做"三步（删空文件、conftest 硬化、启用 `--strict-markers`），把静默通过风险堵住。（**已完成**）
2. 给核心纯函数补 Unit 测试（P0 清单，含升级后的 CAPS 核心判据），这是重构安全网的基础。（**大部分完成**）
3. 用 xfail 测试标记已知 bug 的期望行为，为重构提供明确的"绿灯"目标；可以直接修的坏代码不走 xfail 路线。（**xfail 已落地**；bare except 已改为 `OSError`）
4. 整理现有文件命名和分层，消除语义混乱。（**已完成**）
5. 用黑名单 marker + 合成 fixture 把 CI 策略落地，确保新测试默认入网。（**本地命令可用**；**CI 门禁与 skipped 阈值未接**）

一句话总结：先堵静默通过、再补 Unit、再理分层，不要为已知 bug 写保护性测试，不要用白名单 marker 把未标记测试排除在外。
