# AGENTS.md - TC Probe Design

行为纠偏指南，仅包含模型无法从代码库自行推断的约束。
所有可发现信息（依赖、目录结构、lint规则、CLI入口）请直接读取 `pyproject.toml`。

## 1. 工具链偏好

- 优先使用 `uv`，仅在 `uv` 不可用时回退到 `pip`。
- 质量门：`ruff check` → `ruff format` → `mypy src/tc_probe_design/` → `pytest`。
- 外部工具依赖：部分流程需要 BLAST+（必需）和 PLINK（可选），变更相关行为时需 mock 测试。

## 2. 质量原则

- 此项目对坏代码 **0 容忍**。
- 坏代码包括但不限于：已知的可运行性缺陷、接口漂移、类型错误、测试阻塞、未闭合依赖链、明显结构性劣化、未经控制的性能退化、以及在可以直接使用 Python 标准库或项目内现有能力解决时仍无必要地调用外部工具。
- 对功能开发、缺陷修复和任何可观察行为变更，默认要求按 TDD 开发：先编写失败测试，再做最小实现，再运行测试验证通过。
- 文档、注释、纯格式化、无行为变化的重构不强制 TDD，但仍需提供与改动相匹配的验证。
- 若当前任务客观上不适合 TDD，必须在交付中明确说明原因和替代验证方式，禁止直接跳过且不说明。
- 发现坏代码时，默认要求直接修复；若当前轮次无法修复，必须给出明确、可执行、可追踪的整改方案，禁止以“后续再看”模糊带过。
- 重构不得引入新的坏代码；若发现存量坏代码，必须在交付中显式披露，并说明影响范围、优先级和修复顺序。
- 只要存在 P0/P1 级坏代码，结论即为“当前质量不达标”，不得使用“总体尚可”“可接受技术债”等宽松表述淡化问题。
- 选择外部工具前，必须先证明 Python 标准库或仓库内已有实现无法满足需求；若只是为了图省事而增加 subprocess 依赖，视为坏代码。

## 3. 交付检查

1. `ruff check` + `ruff format` 修复所有违规。
2. 先运行受影响模块的测试，再运行更广泛的测试。
3. 类型敏感变更须通过 `mypy`。
4. 报告实际运行了什么、哪些未能运行。
5. 禁止附带不相关的重构（重命名/移动/格式化噪声）。
6. CLI 行为变更必须同步更新 help text 和测试。

## 4. 编码约束（非显而易见的）

### 必须使用项目枚举，禁止魔法字符串

核心逻辑中不得出现 `"snp"`、`"center"` 等字面量，一律使用 `tc_probe_design.config` 中的枚举：


### 必须使用项目异常体系


### import 风格

- 每个模块顶部必须有 `from __future__ import annotations`。
- 类型专用导入放入 `TYPE_CHECKING` 块。
- 可注入接口使用 `Protocol`。

### 中文注释


## 5. pyproject.toml 是唯一事实来源

当本文件与 `pyproject.toml` 的配置（lint规则、依赖、入口点等）有冲突时，以 `pyproject.toml` 为准。
